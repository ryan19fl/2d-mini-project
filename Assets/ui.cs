using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class ui : MonoBehaviour
{
   // public Button brake;
    public Button temp;
    //public Button gas;
    // Start is called before the first frame update
    void Start()
    {
        //Button btn = brake.GetComponent<Button>();
        //Button btn2 = gas.GetComponent<Button>();
        //btn.onClick.AddListener(forword); btn2.onClick.AddListener(reverse);
            
    }
    public void forword()
    {

    }
   public void reverse()
    { 

    }
    // Update is called once per frame
    void Update()
    {
        
    }
}
